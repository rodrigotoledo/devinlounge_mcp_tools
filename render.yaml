services:
  - type: web
    name: default-settings-mcp
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: MCP_MODE=http node dist/index.js
    healthCheckPath: /health
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
