---
description: Expo UI uses NativeWind/Tailwind via className (keep styles consistent)
globs: hardhat-expo/**/*.{ts,tsx}
alwaysApply: false
---

# Expo (NativeWind / Tailwind) — styling rules

- Prefer **Tailwind utilities via `className`** for layout, spacing, typography and colors.
- Use `style={{}}` only when Tailwind is impractical (dynamic numeric values, `Animated`, APIs requiring inline styles, or matching surrounding patterns).
- Tailwind IntelliSense is configured to use `hardhat-expo/tailwind.config.js` (see `.vscode/settings.json`).
- When you need programmatic colors (e.g. for `Paper`, icons, `ActivityIndicator`), reuse `hardhat-expo/theme/colors.ts`.

